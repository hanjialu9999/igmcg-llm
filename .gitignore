# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments (don't commit)
.my_venv/
.amd_venv/
venv/

# Archived/legacy scripts & data (historical, not part of active project)
archive_unused/

# Pretraining corpus (large data, kept locally not in git)
# 只跟踪目录结构与说明脚本，不跟踪大体量数据文件
data/pretrain_corpus/raw/
data/pretrain_corpus/*.txt
!data/pretrain_corpus/MANUAL.md

# Collected training datasets (个人收集，仅本地保留，不上传 git)
data/datasets/
data/train_data_final.txt

# 本地自测脚本（仅本地运行，不上传 git）
test/

# Model checkpoints & large artifacts (do NOT commit)
*.pt
*.pth
*.ckpt
checkpoints/
checkpoints_smoke_4k/
best_finetuned_model.pt

# Logs
*.log
logs/
logs_smoke_8k/

# 冒烟/实验对比结果（生成质量仅验证机制，不入库）
experiments/smoke_out.txt
experiments/smoke_8k_gen.txt

# 运行期生成的对话参数（由 scripts/tuning/showcase_optimal_params.py 回写）
chat_config.json

# 增强 vs 基线 对比实验产物（全量 merged.txt 受控对比）
# 对比结果文本 + 训练日志入库（可对比）；权重较大（~107MB/份）且不入库，可由 config + data 复现
checkpoints_cmp_enh_full/
checkpoints_cmp_sel_full/
checkpoints_cmp_selv2_full/
logs_cmp_enh_full/
logs_cmp_sel_full/
logs_cmp_selv2_full/

# OS / editor
.DS_Store
Thumbs.db
*.swp
